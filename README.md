# bit_log 

**Визуализатор работы логических элементов на Си**

Проект создан для наглядного изучения работы логических элементов (AND, OR, XOR сдвиги).
Я новичок пришла идея будущего проекта. 


Программа принимает два числа и логический элемент.
1. Вычисляет результат.
2. Раскладывает каждое число на биты.
3. Визуализируем процесс в концоли.

### Поддерживаемые операции:
* '&' - И
* '|' - ИЛИ
* '^' - Исключающее ИЛИ

### План развития 

0. v0 - скелет.
1. v1 - оптимизация.
2. v2 - версия для смартфона.
3. v3 - версия для планшета.
4. v4 - принтовать по вертикали.
5. v5 - по вертикали со смещением.

### Версия v0_skeleton 

Я молодец с начало цикл потом условие. Нужно чтоб код падал (спускался). На циклах и условиях процессор и так прыгает. Но условие в цикле это что то жёсткое( есть второй проект по скорости там надо это проверить). С условий (if, else) выпадаем в цикл. Ну да ладно это скелет нужно было зафиксировать идею.

### Версия v1_bit_calc 

Исправил циклы и условия. Замена (if,else) на (switch). Реализована цветовая индикация разрядов. Зеленый — первое число, синий — второе, желтый — результат операции. Оптимизация. Отступы между байтами. Макросы. Добавил операторы '<<  >>'.

### Версия v2_bit_calc 

**Врсия для смартфона**

Пишу на смартфоне и под смартфон. Эта версия  16 разрядная узкий экран. Причём здесь смартфон termux. Если убрать отступы 32 разряда вместится, но биты сливаются. Можно перевернуть экран тогда с пробелами 32 разряда лягут, но в этом случае нужно убирать экранную клавиатуру. Как не крути не верти мне не комфортно. Поэтому версия v_2 16 разрядная. Красный цвет режет глаза. С шапкой пришлось повозиться. 

     **Добавил**
     операторы (+, -, /, * ,~)
     библиотека stdint.h
     бесконечный цикл wile
     шапку |   |   |   |
     защита от неверного символа 
     с правой стороны dec и оператор

     **Интерфейс*
     При работе с инвертором ( ~ ) нужно добовлять 0 потому, что if () != 3.


### Версия v3_bit_calc

**Версия для планшета**

При копировании допустил ошибку затёр file.c. Пришлось писать код с нуля. Скомпилировал запустил, но при вводе не верного символа цикл уходил в бесконечность.                Ошибку нашёл :
    
    if(scanf(...) != 3) уходил в бесконечность.

    if(scanf(...) != 3) return 1; Ок. 

Консоль это не графика, в которой есть корзина можно откатиться назад. Казалось бы добавь шапку и всё. Думал ни чего интересного не будет. Да писал на планшете.


### Версия v4_bit_calc 

**Версия выводит по вертикали**

До 4 версии утилита выводила по горизонтали. Важно уточнение пишу на Си наAndroid в Termux. Вeрсия v4 написал код который принтует по вертиуали. Смартфон, планшет есть ограничение для вывода колличество разрядов по ширине экрана (горизонтальный вывод).  


### Версия v5_bit_calc

**Принтуем по вертикали со смещением**

README.md нужно дописать.



    
    





